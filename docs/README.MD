# Energy Management Project
A Python-based system for optimizing EV charging with renewable energy integration.

## Setup
1. Install dependencies: `pip install -r requirements.txt`
2. Run simulation: `python scripts/run_simulation.py`

## Structure
- `src/`: Source code
- `tests/`: Unit tests
- `data/`: Input data and configurations